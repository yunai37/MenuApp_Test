<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MenuApp_Test"
        tools:targetApi="31">
        <activity
            android:name=".GateActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".JoinAllergieActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SurveyActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".MypageActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".RecommendActivity" />
        <activity android:name=".RecommendResActivity" />
        <activity android:name=".PopupPW" android:theme="@android:style/Theme.Dialog" />
    </application>
</manifest>